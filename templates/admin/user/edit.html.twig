{% extends 'base.html.twig' %}

{% block adminlte %}{% endblock %}

{% block title %}{{ 'admin.user.edit'|trans }} {{ user.firstName }} {{ user.lastName }}{% endblock %}

{% block body_admin %}
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <div class="content-header">
            <div class="container-fluid">
                <div class="row mb-2">
                    <div class="col-sm-6">
                        <h1 class="m-0">{{ 'admin.user.edit'|trans }} {{ user.firstName }} {{ user.lastName }}</h1>
                    </div><!-- /.col -->
                    <div class="col-sm-6">
                        <ol class="breadcrumb float-sm-right">
                            <li class="breadcrumb-item"><a href="{{ path('admin_dashboard') }}" title="{{ 'home'|trans }}">{{ 'home'|trans }}</a></li>
                            <li class="breadcrumb-item"><a href="{{ path('admin_user_index') }}" title="{{ 'admin.user.viewall'|trans }}">{{ 'admin.user.viewall'|trans }}</a></li>
                            <li class="breadcrumb-item active">{{ 'admin.user.edit'|trans }} {{ user.firstName }} {{ user.lastName }}</li>
                        </ol>
                    </div><!-- /.col -->
                </div><!-- /.row -->
            </div><!-- /.container-fluid -->
        </div>
        <!-- /.content-header -->
        <div class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-6 offset-md-3">
                        <div class="card card-secondary">
                            <div class="card-header">
                                <h3 class="card-title">{{ 'admin.form.titleEdit'|trans }}</h3>
                            </div>
                            <div class="card-body">
                                {{ form_start(form) }}
                                <div class="form-group">
                                    {{ form_label(form.firstName, 'user.form.firstname') }}
                                    {{ form_widget(form.firstName, {'attr': {'class': 'email-bt'}}) }}
                                </div>
                                <div class="form-group">
                                    {{ form_label(form.firstName, 'user.form.lastname') }}
                                    {{ form_widget(form.lastName, {'attr': {'class': 'email-bt'}}) }}
                                </div>
                                <div class="form-group">
                                    {{ form_label(form.firstName, 'user.form.email') }}
                                    {{ form_widget(form.email, {'attr': {'class': 'email-bt'}}) }}
                                </div>
                                <div class="form-group">
                                    {{ form_label(form.firstName, 'user.form.phoneNumber') }}
                                    {{ form_widget(form.phoneNumber, {'attr': {'class': 'email-bt'}}) }}
                                </div>
                                <div class="form-group">
                                    {{ form_label(form.roles, 'user.roles') }}
                                    {{ form_widget(form.roles, {'attr': {'class': 'email-bt'}}) }}
                                </div>
                                <div class="form-group">
                                    <button title="{{ 'save'|trans }}"
                                            class="btn btn-block btn-info mt-3  mb-3"><i class="fas fa-save"></i> {{ 'save'|trans }}</button>
                                    <a href="{{ path('admin_user_index') }}" class="btn btn-default" title="{{ 'back'|trans }}"><i class="fas fa-backward"></i> {{ 'back'|trans }}</a>
                                </div>
                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascript_admin_additionnals %}
    {# Script bsCustomFileInputjs #}
    {{ encore_entry_script_tags('bsfileinput') }}
{% endblock %}
