{% extends 'base.html.twig' %}

{% block title %}OrderDetails{% endblock %}

{% block body %}
    <h1>OrderDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_detail.id }}</td>
            </tr>
            <tr>
                <th>QuantityProduct</th>
                <td>{{ order_detail.quantityProduct }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ order_detail.price }}</td>
            </tr>
            <tr>
                <th>Price_ttc</th>
                <td>{{ order_detail.priceTtc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_details_index') }}">back to list</a>

    <a href="{{ path('order_details_edit', {'id': order_detail.id}) }}">edit</a>

    {{ include('order_details/_delete_form.html.twig') }}
{% endblock %}
