{{ form_start(form) }}
<div class="row">
    <div class="form-group col-md-4">
        {{ form_label(form.product, 'comment.placeholder.product') }}
        {{ form_widget(form.product) }}
    </div>
    <div class="form-group col-md-4">
        {{ form_label(form.isModerate, 'comment.placeholder.isModerate') }}
        {{ form_widget(form.isModerate) }}
    </div>
        <div class="form-group col-md-4">
            {{ form_label(form.rating, 'comment.placeholder.rating') }}
            {{ form_widget(form.rating) }}
        </div>
</div>
<div class="row">
    <div class="form-group col-md-12">
        {{ form_label(form.message, 'comment.placeholder.message') }}
        {{ form_widget(form.message) }}
    </div>
</div>
<div class="form-group">
    <button title="{{ 'save'|trans }}"
            class="btn btn-block btn-info mt-3  mb-3"><i class="fas fa-save"></i> {{ 'save'|trans }}</button>
    <a href="{{ path('admin_comment_index') }}" class="btn btn-default" title="{{ 'back'|trans }}"><i class="fas fa-backward"></i> {{ 'back'|trans }}</a>
</div>
{{ form_end(form) }}

